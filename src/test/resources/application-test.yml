spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobtracker_test
    username: jobtracker_test_user
    password: test_password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  
  sql:
    init:
      mode: never

jwt:
  secret: test-jwt-secret-key-test-jwt-secret-key
  expiration: 900000
